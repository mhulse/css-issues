title: "CSS Bullets"
description: "A copy and paste selection of handy CSS code snippets. Useful for when quick, drop-in, solutions are needed."
exclude:
  - "*.rb"
  - "Gemfile*"
  - "LICENSE"
  - "README.md"
  - "DEV.md"
safe: "true"
collections:
  pages:
    output: "true"
    permalink: "/:path"
defaults:
  -
    scope:
      path: ""
    values:
      layout: "default"
  -
    scope:
      path: "_pages"
      type: "pages"
    values:
      layout: "pages"
